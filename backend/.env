# Database
DATABASE_URL="file:./prisma/dev.db"

# JWT Secrets (change in production!)
JWT_SECRET="sparksupport-super-secret-jwt-key-local-dev-2025"
JWT_REFRESH_SECRET="sparksupport-super-secret-refresh-jwt-key-local-dev-2025"
JWT_EXPIRES_IN="1h"
JWT_REFRESH_EXPIRES_IN="7d"

# Server
PORT=8000
NODE_ENV="development"

# Security
BCRYPT_ROUNDS=12

# Email Configuration (RESEND)
RESEND_API_KEY=your_resend_api_key_here
FROM_EMAIL=onboarding@resend.dev
FROM_NAME=SparkSupport Team
FRONTEND_URL=http://localhost:5173
EMAIL_ENABLED=true
